---
kind: pipeline
type: docker
name: x86

platform:
  arch: amd64

steps:
- name: build
  image: plugins/docker
  settings:
    repo: blafy/caddockery
    tags: latest-amd64
    username: blafy
    password:
        from_secret: dockerpwd

---
kind: pipeline
type: docker
name: arm8

platform:
  arch: arm64

steps:
- name: build
  image: plugins/docker
  settings:
    repo: blafy/caddockery
    tags: latest-arm64
    username: blafy
    password:
        from_secret: dockerpwd
